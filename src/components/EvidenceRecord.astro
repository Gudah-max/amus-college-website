---
interface Props { heading: string; copy: string; }
const { heading, copy } = Astro.props;
---
<section class="evidence-record"><div class="wrap evidence-record-inner"><p class="section-kicker">Results &amp; achievements</p><div><h2>{heading}</h2><p>{copy}</p></div></div></section>
